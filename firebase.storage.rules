rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: These rules are for testing only. Do not use in production.
    match /{allPaths=**} {
      allow read, write;
    }
  }
} 